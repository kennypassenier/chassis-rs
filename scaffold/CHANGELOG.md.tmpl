# Changelog

All notable changes to {{ name }}. Semantic versioning: a breaking change
in the API, the configuration knobs or the on-disk state is a major, and
carries a **Migration** section saying what an operator must do.

## [Unreleased]

### Added
- Project created with `chassis new` (chassis {{ chassis_tag }}).
