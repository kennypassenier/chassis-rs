# Realization plan — chassis-rs

Phase 5 output, drafted 2026-09-05 during the AFK run; ratified in round
**R5** (docs/PENDING_MINI_ROUNDS.md). Feature IDs: docs/FEATURES.md.
Decisions: docs/ARCHITECTURE_DECISIONS.md.

## Ground rules for this plan

- **Enforcement before feature code.** L0 ends with the git-native hooks
  live (`.githooks/` via `core.hooksPath`, `.claude/hooks/gates.sh` +
  `check-commit.sh`), CI green on `main`, and branch protection
  requiring the CI check (workflow runs on all branches, rule 6a).
  Hook configuration per Kenny's Q9 (2026-09-05): fmt, clippy with
  warnings as errors, full test suite, clean-tree check, IDs in the
  message, `--no-verify` forbidden; cargo-deny in CI only.
- **Scratch resource (rule 14):** CT 118 `118-app-chassis` on the Proxmox
  host 10.10.5.250, ip 10.10.10.18, Debian 13, unprivileged, disposable.
  Every milestone that touches systemd, files or the network runs at
  least one live drill there (L5 rule) and the drill's outcome is report
  evidence.
- **Credential-touching steps (rule 21):** none inside the AFK run. The
  minisign signing key stays with Kenny (release = deliberately not done);
  `gh` calls that create repositories are drilled with `--dry-run` only,
  the live `chassis new` remote drill waits for Kenny.
- **Assembly is its own milestone (L7):** the exit criterion is that the
  example service does its own job end to end, not that the parts exist.

## Milestones

| Milestone | Features | Exit criteria | Why this order |
|---|---|---|---|
| **L0 · Walking skeleton** | — | Workspace with `crates/chassis` (lib), `crates/chassis-cli` (bin), `examples/inbox` (bin); `rust-toolchain.toml`, `deny.toml`, `.gitignore`; CI (fmt · clippy · tests · deny · coverage-informational) green on `main`; hooks live and **proven by firing once** (rule 7d: a commit with a clippy warning is refused, a commit without an ID is refused); branch protection read back (rule 13a). | Nothing is built on an unenforced tree. |
| **L1 · Core lifecycle** | K2, K3, K4, K5, K11, W1, W9 (AR2, AR3, AR4, AR13, AR15, AR20) | `App::from_env_and_args` with the precedence table test (flag > env > file > default), `${VAR}` expansion, one state root; `--version`, `--check`, `--print-config` proven to open no socket and touch no state; access log with request-id; SIGTERM E2E against the real binary (exit 0, in-flight completes, second signal no-op); `sd_notify` READY after bind. **Drill:** the glibc binary copied to CT 118 runs `--version` and `--check` under the target OS. | Everything else mounts on this. |
| **L2 · Health, metrics, guards** | K6, K7, K10 (AR12) | `/healthz` with `version` and subsystem registry (503 on degraded); `/metrics` baseline + project registry with a name-preservation test; `--healthcheck` exits 0/1; body limit 413, in-flight 503 + Retry-After, rate limit 429, CSRF 403; shipped defaults pass their own validation. **Drill:** Prometheus text parsed by `promtool check metrics` on the PC. | Observability before state, so later drills can be measured. |
| **L3 · Stores, login, clients** | K8, K12, K13, K14 (AR5, AR6, AR7, AR9) | Encrypted file store v1 with N/N−1 read test; atomic writes; `ClientStore` trait with file + in-memory implementations under one suite (rule 7g); login/session/remember-me/logout E2E; two-secrets startup rules with generated candidates; client lifecycle over HTTP; captures ring with redaction, truncation, TTL, `last_used_at`; test-request endpoint. Plaintext-scan test over the whole suite's output. **Drill:** store files written on CT 118 survive a `systemctl restart` (sessions still valid). | Auth and state before any HTML. |
| **L4 · Dashboard** | K15, K16, K17, K9 (AR11) | kp-themes v3.1.0 vendored with checksum gate; layout blocks + registry (nav, pages, status sections, client columns/actions) exercised by a test project; explain-block lint; status page fields shown to take two values; clients page with the three buttons, arm-before-act and busy states; passkeys register/assert with a software authenticator over a simulated HTTPS request and 404 over plain HTTP. **Publish the visible surface** (PROCEDURE Phase 6): the dashboard runs on CT 118 behind its login so Kenny can open it on his return. | Chrome last among the in-process parts, on top of real data. |
| **L5 · Self-update and notifier** | K18, K19, K20, K21, K22 (AR8, AR9, AR10, AR19) | Update decision table unit tests (Almanac's nine cases ported); fake release server in `chassis::testing`; supervised: swap + exit 0, second run exit 0 untouched; autonomous: rollback after failed starts with a paused clock; hold; drill flag; state copy before swap; signature verified before any hash, pinned real vector; notifier fan-out, fallback chain, `${VAR}` secrets never logged. **Drill on CT 118:** supervised update from a release server on the PC (`<P>_UPDATE_URL`), then the autonomous rollback with a deliberately failing start. | The riskiest mechanism gets the most drill time, after the platform it updates exists. |
| **L6 · Scaffold CLI** | K23 (AR14, AR16, AR17, AR18) | `chassis new` renders a project that builds and passes its own gates; `sync` zero-diff after `new`, one diff after a template change; `release --dry-run` walks every step against a local fake `gh`; golden unit passes `systemd-analyze verify`; `service.yml` paths cross-checked against the unit; Dockerfile builds. **Drill on CT 118:** the scaffolded unit installed for inbox, `systemctl start` blocks until healthy (W9), `ExecStartPre --check` refuses a broken env. | Templates need the real binary's flags to exist first. |
| **L7 · Assembly and example** | K24, K25, K26 | `examples/inbox` uses every kit module: token login, client issued, `POST /v1/messages` with the token, message on the page, capture on the row, status section count, `message.received` notification received by a local webhook; `examples/minimal.rs` ≤ 40 lines with a line-count test; rustdoc lint (every public module opens with a sentence); `cargo semver-checks` informational in CI; CHANGELOG with a Migration heading rule. **Exit criterion in one sentence:** a person can go from `chassis new inbox` to a message visible on the dashboard following only the README. | The milestone HTTPSwitchboard forgot. |
| **L8 · Live drills for the release report** | C1–C4, M3 | On CT 118 with the real binary: supervised broken-release drill (verifies, fails at start, homelab-style restart + `is-active` polling, rollback restores `bin.prev`); autonomous broken-release drill; restore drill (destroy state, restore the tar, log in with an existing client token); C3 proof (kp-themes bump → kit release candidate → example rebuilt with a tag change); C4 read-through prepared for Kenny. Everything that needs Kenny (signing, `homelab adopt`, remote `chassis new`, passkeys behind Traefik) listed in the queue. | Proof before the release gate. |

## What waits for Kenny (deliberately not done in AFK)

See docs/PENDING_MINI_ROUNDS.md §Deliberately not done: signing and
publishing, `homelab adopt` of CT 118, the passkey test behind Traefik,
the live `chassis new` remote drill (creates a repository), and anything
that would change a frozen decision.

## Status

| Milestone | Status | Evidence |
|---|---|---|
| L0 | done 2026-09-05 | commit `d9679e7`; hooks fired red on an injected clippy warning and on a message without IDs before the green commit; CI run 33949831302 green on gates + coverage, cargo-deny red on a versionless path dependency (fixed in L1's commit); branch protection: pending the next green run |
| L1 | done 2026-09-05 | `cargo test --workspace --all-features`: 21 unit tests in `chassis` (precedence table, `${VAR}` fail-closed, secret-in-file refused, request-id generated/echoed, error JSON with remedy, internal message hidden, shutdown bound, port-0 start + flush after drain, `--version` reads no config, `--check` runs project hooks) + 4 E2E in `examples/inbox/tests/lifecycle_e2e.rs` against the real binary (`--version` with no configuration, `--print-config` sources + `--check` touches nothing, SIGTERM → exit 0 with a second signal harmless, in-flight request completes during drain). Live-found while testing: ANSI colour codes in stderr off-terminal → fixed (`with_ansi(is_terminal)`), test child reaped on failure. **Drill on CT 118 done 2026-09-05** with the Debian-trixie build (`docker run rust:1.97-slim-trixie cargo build --release -p inbox`, 9.0 MB, glibc 2.41 on the container): `--version` → `inbox 0.1.0`, `--check` → `configuration ok`, `--print-config` shows `(flag)`/`(env)` sources, served on 127.0.0.1:18080, SIGTERM → exit 0, `drained=true` in the log. |
| L2 | done 2026-09-05 | commit `8c82748`; 32 unit tests + 4 E2E (`cargo test --workspace --all-features`): /healthz version + subsystems + 503 on degraded, probe alive on 200 and 503 (critic #3), /metrics build_info + counted requests by matched route + verbatim scrape source, 413 body cap, 503 in-flight cap with Retry-After, 408 timeout with exempt prefix, CSRF 403/200, keyed limiter burst, trusted-proxy IP/HTTPS detection, shipped defaults pass validation. **Drill on CT 118:** `inbox --healthcheck` against the running trixie binary → `alive=true status=ok version=0.1.0`, exit 0. CI: gates green; cargo-deny red on a license (fixed in the L3 commit). |
| L3 | done 2026-09-05 (drill pending) | 50 unit tests + 6 E2E (`cargo test --workspace --all-features`): sealed store round-trip with plaintext never on disk, wrong key → remedy names `rekey`, envelope version refused, file + memory `ClientStore` under one suite (rule 7g) incl. reopen, clients lifecycle (issue/re-issue/revoke/delete, name freed after revoke, constant-time lookup), sessions slide vs remember-me fixed + hashed at rest, captures redact/truncate/ring/TTL, secrets both-or-neither with `gen-secret` remedy and no value in the message, `gen-secret` refuses a pipe. E2E `client_token_flow_end_to_end`: redirect to /login (303, not 401), wrong token 200 + message, HttpOnly SameSite=Lax cookie not Secure on plain HTTP, issue (201) with token absent from the list, reveal 64-hex + curl command, 401 without token, 202 with token, capture shows `authorization: ***` and never the token, test button adds a `test` capture, `last_used_at`/`uses` move, revoke refused within the same second with the row kept inactive, logout kills the session, **plaintext scan over the state dir finds neither secret**. `--check` and start refuse without secrets (W6). Commit `7856b0d`. **Drill on CT 118 done 2026-09-05** (trixie build, env file with both secrets, listening on 0.0.0.0:18080, driven from the PC over the LAN): login 303, client issued, token accepted (202); SIGTERM + restart; the same cookie still lists clients (200), the same token is still accepted (202), `uses=2` persisted across the restart, `clients.json.enc` + `sessions.json.enc` are the only files in the state dir, and `grep` for the token over the state dir and the log finds nothing. |
| L4 | done 2026-09-05 (pages + passkeys) | **K9:** `webauthn-rs` 0.5.5 behind the `passkeys` feature (pulls OpenSSL, T8); relying party from `<P>_PUBLIC_URL` (critic #5; https required, refused at parse when malformed, required at `--check`/start); ceremony state in a bounded in-memory map with a 300 s TTL; credentials in `passkeys.json.enc`; routes 404 with a remedy unless the request is HTTPS via a trusted proxy; registration needs the admin session, login with a passkey opens the same session the token would; `/passkeys` page + `passkeys.js` for the browser ceremonies. Tests: PUBLIC_URL validation, challenge issued, garbage finish refused; E2E `passkeys_exist_only_over_https_from_a_trusted_proxy` (404 + remedy on plain HTTP, spoofed header ignored, button + `Secure` cookie + challenge with `rp.id` over simulated HTTPS). The real browser ceremony is on the deliberately-not-done list (Traefik hostname is Kenny's). | 56 unit + 7 E2E green (`cargo test --workspace --all-features`). K15: kp-themes **v3.1.0** vendored (8 files) with `static/kp/KP_THEMES.sha256` and a gate test that compares every embedded copy against the release manifest; assets served at `/static/{name}?v=<fnv hash>` with `immutable` caching, traversal-proof lookup. K16: minijinja layout with `nav`/`content`/`head` blocks, theme picker rendered server-side from the vendored registry (parsed, not typed — 24 themes), explain-block lint test, `App::nav_entry / status_section / client_column / problems / clients_label / dashboard_routes`. K17: status page with service, health (per subsystem), update card (placeholder until L5), problems card, project sections. Clients page: issue form, per-row Reveal (N s) / Copy token / Copy command / Last requests / Send test / Re-issue / Revoke / Delete with `data-kp-confirm` arm-before-act and `aria-busy` (rule 31); the token never appears in page HTML (E2E asserts no `Bearer `). Wrong login re-renders the page with a `kp-alert` at HTTP 200. Commit `8b518d2`. **Drill on CT 118 done 2026-09-05:** the trixie build installed as `/usr/local/bin/inbox`, a dedicated `inbox` user, `/etc/inbox/inbox.env` (0640) with both secrets, and the golden unit from AR17 verbatim (`Type=notify`, `ExecStartPre --check`, `StartLimit*` in `[Unit]`, hardening, `ReadWritePaths` over state + binary dir): `systemd-analyze verify` silent, `systemctl enable --now` → `active`, `NRestarts=0`, `Type=notify` (W9 proven live: the unit reached active only after READY=1), `--healthcheck` alive, login page 200 from the PC over the LAN. **Published for Kenny** at http://10.10.10.18:8080 (see PENDING_MINI_ROUNDS.md for the token's location). |
| L5 | done 2026-09-05 (live update drill waits for Kenny's signature) | **Update (K18–K21):** `core::update` ports Almanac's decision table (7 tests: numeric versions, strictly newer, hold pin/skip, manifest lookup constant-time, modes + OCI-only container detection with the LXC case, startup table incl. stale state, version-bound drill marker); `shell::update` (7 tests with a signed fake release server): supervised swap with `link+rename`, second run touches nothing, a release failing its own `--check` is never installed and its stderr is quoted, a foreign signature is refused before any hash and a wrong hash after, hold and pending state block, autonomous writes state → probation → revert restores `bin.prev` → restored version starts clean, `confirm_healthy` clears probation, drill marker consumed once by the named version, corrupt state reads as none, copies pruned. `<name> update` subcommand (exit 0 also when current), `--check` validates mode/hold/drill, `update_url` derives from `AppSpec.repository`, pre-update copies in `<state>-pre-update/`, status card fed by the updater. **Notify (K22):** `[[notify.webhook]]` parsed with `${VAR}` fail-closed, event globs, body templates, method check (2 tests); bounded queue, backon retries with jitter, fallback chain, unwanted events not sent, logging-only when unconfigured (3 tests); kit emits `service.started`, `update.*`, `health.degraded/recovered` (debounced by transition); `App::notifier()` for project events. E2E `update_subcommand_refuses_a_foreign_signature_and_touches_nothing`: the real binary against a release signed by a throwaway key → exit 1, "signature does not verify", binary and state untouched; unreachable host → remedy names `update_url`; `--check` refuses bad mode/drill/hold. **Drill on CT 118:** the live swap needs a release signed with the ecosystem key, so it waits for Kenny (see the queue). |
| L6 | done 2026-09-05 (remote drills wait for Kenny) | `chassis` CLI (K23, AR14): `new <name>` renders 19 scaffold files (Cargo.toml with the kit pinned to a tag or `--chassis-path`, main.rs, README, CHANGELOG, toolchain pin, deny.toml, CI + Release workflows with `GITHUB_TOKEN` only, Dockerfile on trixie, golden unit + latch variant (AR17), homelab `service.yml` with a waiting `update_cmd` (AR18), `sign-release.sh` that uploads `.minisig` before `VERSION` (critic #15), git hooks, Claude hook, settings), `git init` + hooks path + first commit, `gh repo create --push` unless `--no-remote`, `.chassis.toml` records the inputs; `sync` diffs kit-owned files (project-owned ones reported, never overwritten without `--force`), exit 1 on drift for CI, `--protect` applies branch protection and reads it back (13a); `release <version>` bumps, dates the changelog, commits through the gates, pushes a work branch, waits for the commit's checks (6b), fast-forwards main, tags, waits for the Release run by `head_branch == tag`, signs locally. Tests: every template renders with substitutions and the GitHub `${{ }}` guards intact (found the JSON auto-escape on `.yml`), pubkey mirrors the kit, path dependency swap, name/version/changelog rules, `new` → clean `sync` → drift reported and repaired, owned files left alone; **E2E `a_new_project_compiles_and_answers_version`**: the real `chassis new` output builds against this checkout and answers `--version`, `--check` without secrets refuses with `gen-secret`, dry-run release names every step. **Waits for Kenny:** the live `chassis new` with a remote (creates a repository) and `sync --protect` on it. |
| L7 | done 2026-09-05 | **Assembly (K26):** `examples/inbox` uses every kit module — token login, clients, `POST /v1/messages` behind a client token, captures on the row, the test button, a `Messages` status section (count + last five) and a `Messages` client column (K16/K17 extension points exercised), `message.received` notifier event, state-copy hook, self-update with `repository`; E2E asserts the section and the column render. **K24:** `examples/minimal.rs` (one file, 39 lines after rustfmt, enforced by a test that also requires an opening doc comment); a lint test requires every module to open with a `//!` sentence; README with the eject route; CHANGELOG. **K25:** `chassis release` refuses a major bump without a `Migration` section (test); `chassis::testing` deferred — the E2E suites use `App::from_args` + port 0 directly, which is the helper. `cargo semver-checks` waits for a first tag to compare against (noted for Phase 7). Live-found in CI (7f69a1f): a runner has no git identity, so `chassis new`'s first commit failed there — fixed with a per-command identity fallback when none is configured. |
| L8 | partly done 2026-09-05 — the rest needs Kenny | **Done on CT 118 with the L7 trixie build under the golden unit (Type=notify):** restart with the new binary `active`, `NRestarts=0`; **restore drill (M3, C1 half):** a client issued and used, `systemctl stop`, `tar` of `/var/lib/inbox` (only `clients.json.enc` + `sessions.json.enc`), state destroyed to zero files, tar restored, `systemctl start` → `active`, the same client token accepted (202) and the same session valid (200), `uses` continued at 2 — the state root is the whole backup, as M4 promised. **Supervised update against the real default host** (`repository` = this repo, no release yet): exit 1 with the remedy naming the missing asset and that the next check retries — nothing touched. Status page shows the project's `Messages` section live. **Waits for Kenny (queue):** the signed drill release for the supervised swap, the autonomous rollback and `broken-after-ready`; `homelab adopt` of CT 118; passkeys behind Traefik; the remote `chassis new`. C3 (kp-themes bump = one kit release) needs a kit tag first. **Live update drills (afternoon, drill key A1, `/opt/inbox/bin` + hardened unit):** supervised swap 0.1.0→0.1.1 via `systemd-run` exit 0, `.prev` kept, pre-update copy, restart NRestarts=0, second run no-op; autonomous rollback 0.1.1→0.1.2 (`update_drill=broken`): install, crash before READY, revert on the second start — and the churn after the revert (three cycles, NRestarts=6) → CF-3 fixed with `update-skip.json` + red/green test; re-drill 0.1.3→0.1.4 (14:21 UTC): exactly one install/crash/revert (NRestarts=3), then `Held` at +3 s and +63 s, skip list `["0.1.4"]` — CF-3 measured. **Broken-after-ready under the homelab** (adopted per A3, 14:24 UTC): the kit did its part (READY, exit 1 after 5 s); the homelab's installed binary (v3.48.0, older than fix 1ed72e3) declared healthy immediately — announced to the Homelab Rust session for a re-run after reinstalling. CT 118 restored to 0.1.3 supervised; `update_cmd` removed from the stack until a real release pipeline exists (nightly = backup only). |

## Gate log (from Phase 7 onward; earlier gates live in their documents)

| Gate | Date | Decision | Where it landed |
|---|---|---|---|
| Combined ratification R1–R5 + milestone reports L0–L8 (AFK run) | 2026-09-05 | All Akkoord; D4–D8 + N1 Klopt; D1–D3 → deep-dive; a bare "ik" in D7's consequence line → CF-2 | docs/PENDING_MINI_ROUNDS.md (outcome section, R-rows), CLAUDE.md status |
| Deep-dive DD-1 (D1 `/` = status page, D2 request-id in header, D3 global knob flags) | 2026-09-05 | Klopt ×3 | README "Conventions worth knowing"; AR4 amendment; AR11 note |
| Correction form CF-2 (bare pronouns in text read on its own) | 2026-09-05 | C1–C3, C6–C9 Klopt; C4/C5 replaced by Kenny's template "Claude doet… / Kenny doet…", no script; lint = fallback; measured at the Phase 7 form: raw count 1 → fallback active | docs/PENDING_MINI_ROUNDS.md §CF-2; FORM_PROTOCOL §7 (dev-procedure dd4f66b); central memory |
| Phase 7 hardening form (H1–H17 test gaps + drills, S1–S9 security) | 2026-09-05 | Dichten: H1–H6, H8–H17, S1–S5, S7, S8 (H4 with Kenny's addition: log rotation must exist where the service runs; H16 = Kenny gives tag/key/go now; H17 = SHA pins). Later: H7 (passkey success path — live Bitwarden test first). Accepted as known limitation: S6 (passkey ceremony table, re-raise at the retro), S9 (by-design notes). Remark: explain the minijinja dashboard model with Almanac examples | docs/TEST_PLAN.md (Phase 7 output), this table, the code commits tagged [H…]/[S…] |
| Phase 7 hardening build (batches 1–2) | 2026-09-05 | Built as decided: S1 signature↔version binding + https rule + download cap; A1 `update_pubkey` trust-root override (logged, on the card); H9 read-only version watch; H14 paused-clock loop test; H5 Almanac v2.4.0 fixture; H11 state-dir probe + store subsystem; H12/S4 startup warnings, mode log, untrusted-proxy problem entry; S3 413 on captured routes + bare 413 → JSON; S5 host-only webhook logging; H2 `rekey` + remedy-existence test; H3 per-token limiter + 429 E2E; S8 secrets off argv, limiter pruning, request-id sanitising, 0600 store files, security headers, vendored fonts + theme-boot.js, `.dockerignore`, CLI input validation, logout cookie flags, webauthn danger feature dropped; S7 `--locked`, read-only CI token; H17 SHA-pinned actions + bind-mount note; S2 `/opt/<name>/bin` + hardening set; H4 log-scan/JSON/access-count E2E + journald drop-in + compose logging; H6 notifier E2E; H8 restart E2E; H13 ten tests made falsifiable; H15 bundle (readyz, reissue/delete, backoff bounds, latch unit, literal-port scan, `${VAR}` masking, K13 bar); H10 generated project runs its own gates + systemd-analyze (found: template not rustfmt-clean, lockfile missing from the first commit — fixed); K16 project pages via `Dashboard` extension + inbox `/messages`. Live-found during the build: SIGTERM listener registered after bind (race under load) — named and fixed (rule 8a). Suite: 93 unit + 7 CLI + 1 scaffold E2E + 21 inbox E2E (`cargo test --workspace --all-features`). | docs/TEST_PLAN.md; commits f116456 and the batch-2 commit |
| Phase 7 close-out (R7), Phase 8 docs (R8), Phase 9 release 1.0.0 (R9) — crossed AFK | 2026-09-05 | Kenny's AFK round 2 (A5 = go for the tag after green Phase 7+8 and the drills; A6 = gates become ratification rounds). Tag `v1.0.0` on main after CI; GitHub release with the CHANGELOG 1.0.0 section; no signing needed (library + CLI via git tag) | docs/PENDING_MINI_ROUNDS.md (AFK round 2 section, R7–R9 queued), CHANGELOG.md [1.0.0], the tag |
| Releases 1.1.0 and 1.2.0 — crossed AFK | 2026-09-05 | 1.1.0: `on_start`, `knob_keys`, public `spec` (kyu-runner migration). 1.2.0: `help_extra`, `needs_project_config`, `update_gate`, `--help` exits 0 (`Control::Help`, red test first), `assets` feature (http-switchboard, kyu, Almanac migrations). Each: release branch → CI green → fast-forward `main` → tag → GitHub release. Kit suite at 1.2.0: 98 unit + 7 CLI + 1 scaffold E2E + 21 inbox E2E. Ratified as part of R9. | CHANGELOG [1.1.0], [1.2.0]; docs/MIGRATION.md; PENDING §Phase 9 |
| Migration branches (A2) — kyu-runner 0.2.0 (`eded033`, `e674eb1`, 69 tests), http-switchboard 2.0.0 (`ddbe11e`, 99 tests incl. docker E2E), kyu 3.0.0 (`1ca1e08`, 190 tests), Almanac 3.0.0 (`6e36b7f`, 306 tests) | 2026-09-05 | Built AFK on `chassis-migration` branches, each through the project's own gates; no release, no deploy. Scope decisions taken AFK (kyu and Almanac keep their own dashboards; self-update on for kyu-runner and http-switchboard per A7) and the regressions found are items in the combined ratification form. Two live-found kit faults fixed on the way (`--healthcheck` reading project config; `--help` as a refusal). | docs/PENDING_MINI_ROUNDS.md §Migration branches; each project's CHANGELOG Migration section |
| Combined ratification AFK run 2 (R7, CF-3, R8.1–R8.6, R9, MIG-1…4, D-K1, D-H1, D-A1, H16, HL-1, HL-2, CF-2.M) | 2026-09-05 | 28 items answered. Phase 7 closed, the six documents approved as written, the three releases signed off, the four migration branches accepted as branches (no release, no deploy). CF-3 closed with all nine fields Klopt; CF-2's fallback lint confirmed as this project's working method (0 bare pronouns over 196 loose text lines). Three items adopted as NEW work: D-K1 kyu takes the kit dashboard in a second step, D-H1 http-switchboard folds its own events into the kit logger, D-A1 kit 1.3.0 gains `on_update_event` and Almanac wires its Home Assistant vocabulary onto it. | docs/PENDING_MINI_ROUNDS.md §Combined ratification AFK run 2; CLAUDE.md status |
