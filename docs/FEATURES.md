# Features — chassis-rs

Phase 2 output. Ratings were taken by Kenny on 2026-09-05 in the
starterskit session (28-item form plus deep-dive round); this document
assigns the permanent IDs and the concrete test bar per feature. The
freeze is ratified in round **R2** (docs/PENDING_MINI_ROUNDS.md). After
the freeze, changes go through mini-rounds only.

Scale: **Essential** (kit 1.0, before the first existing project
migrates) · **Desired** (a 1.x release after the Essential list is
empty) · **Later** (registered, no hook in the kit) · **Don't do**
(dropped with Kenny's reason).

Every feature names its **test bar** now, so Phase 7 audits against a
sentence written before the code existed. "E2E" means a test that drives
the real binary or the real axum router over HTTP; "unit" means the pure
function.

## Round 1 — Kenny's features (K)

| ID | Rating | Feature | Test bar |
|---|---|---|---|
| K1 | Essential | **axum as the one web layer.** Health, metrics, API and dashboard all mount on one axum 0.8 router the kit builds; the project merges its own router into it. | E2E: a project router merged under `/v1` answers next to `/healthz` in one process. |
| K2 | Essential | **Configuration layering and control commands.** Precedence CLI flag > env var > config file > default; prefix derived from the app name (`INBOX_LISTEN`); `${VAR}` references in the file resolve from env; one state root (`<PREFIX>_STATE_DIR`) and every path derived from it, no per-path overrides; `--version`, `--check` and `--print-config` exit without opening a socket or reading secrets they do not need; `--print-config` shows every effective value with its source and secrets as `***`. | Unit: precedence table (4 sources × 3 knobs). E2E: `--print-config` output contains `source=env` for an overridden knob and never a secret's value; `--version` succeeds with no config and no state dir. |
| K3 | Essential | **Errors with a remedy, one JSON error shape, a Secret type.** The kit's error type requires a `remedy` string at compile time; every API error renders `{"error": …, "remedy": …}` with the mapped status; `Secret<T>` prints `***` in Debug and Display and exposes its value only through one named method. | Unit: constructing an error without a remedy does not compile (trybuild or a doc-test that must fail). E2E: an unauthorised request returns the JSON shape. Plaintext-scan test: no secret value in any log line or response body of the suite. |
| K4 | Essential | **Logging with tracing.** `tracing` + `EnvFilter` from `<PREFIX>_LOG` (default `info`), text to stderr, JSON when `<PREFIX>_LOG_FORMAT=json`. Payloads and secrets never reach a log line. | E2E: a request produces exactly one access line at `info`; the JSON mode parses as one JSON object per line. |
| K5 | Essential | **Graceful shutdown (norm N1).** SIGTERM or Ctrl-C: stop accepting, finish in-flight requests, run the project's flush hook, bounded by `<PREFIX>_SHUTDOWN_TIMEOUT_MS` (default 10000, refuses 0 or a non-number), exit 0 always, a second signal changes nothing. | E2E against the real binary: start, hold a slow request open, send SIGTERM, assert the request completes and exit code is 0; send two SIGTERMs, assert one clean exit. |
| K6 | Essential | **`/healthz` with version and subsystems.** Open JSON: `{"status": "ok"|"degraded", "version": "<cargo version>", "subsystems": {...}}`; a project registers subsystem checks; degraded returns 503 only when a check says so. No `/readyz`. | E2E: the `version` field equals `CARGO_PKG_VERSION`; a registered failing check flips status and code. |
| K7 | Essential | **`/metrics` with a kit baseline and a project registry.** Prometheus text: `<prefix>_build_info`, `<prefix>_uptime_seconds`, `<prefix>_http_requests_total{route,status}`; the project registers its own counters and gauges in the same registry; existing metric names of migrating projects can be kept verbatim; every metric carries a `blind_spot` note in the docs. `--healthcheck [url]` probes the process's own `/healthz` without curl. | E2E: scrape parses with a Prometheus text parser; counters never decrease across ten requests; `--healthcheck` exits 0 against a running instance and 1 against a closed port. |
| K8 | Essential | **Token login with sessions, two secrets.** Bootstrap token (`<PREFIX>_TOKEN`, ≥16 chars) compared constant-time; session cookie `<name>_session` HttpOnly, SameSite=Lax, optional remember-me (30 days); sessions persist in the encrypted store across restarts; wrong token → 200 with an inline error (never 401 to a browser); `<PREFIX>_SECRET_KEY` (32 bytes hex) encrypts the client store; one secret set without the other is a startup error whose message contains a freshly generated valid value; **no** unprotected mode: a dashboard without both secrets refuses to start (W6 = Don't do). | E2E: login flow with cookie, restart, cookie still valid; rotating `<PREFIX>_TOKEN` keeps client tokens working; missing one secret → exit 1 with a 64-hex candidate in the message. |
| K9 | Essential | **Passkeys behind Traefik.** WebAuthn registration and login (`webauthn-rs`), offered only when the request arrived over HTTPS (proxy header or TLS-terminating proxy); first passkey registered after a token login; credentials stored in the encrypted store. TLS is never terminated in-process (W8 = Don't do). | E2E with a software authenticator (webauthn-rs test tools): register + assert; over plain HTTP the passkey routes answer 404 and the login page hides the button. |
| K10 | Essential | **Guards with knobs.** Same-origin CSRF guard on state-changing requests (Origin vs Host, kyu's rule); rate limiting on `/login` and per client token on the API (`<PREFIX>_RATE_LIMIT_*`); body size and in-flight concurrency limits configurable (`<PREFIX>_MAX_BODY_BYTES`, `<PREFIX>_MAX_IN_FLIGHT`), 413 / 503 + Retry-After when exceeded. | E2E: cross-origin POST → 403; N+1 login attempts → 429; body over the cap → 413; shipped defaults pass their own validation (HTTPSwitchboard rule). |
| K10 (amendment 2026-09-06, CF-7) | — | **The CSRF rule reads `Sec-Fetch-Site` first** (`same-origin`/`none` pass, `cross-site`/`same-site` refused), `Origin` vs `Host` only without it; the security headers carry `referrer-policy: same-origin`, not `no-referrer`. Found live on CT 112: under `no-referrer` a browser form submit sends `Origin: null`, and the Origin-only rule refused every dashboard form from Chrome, login included. Refusals to a browser navigation render in the layout (`error.html`) instead of a bare JSON tab. | unit `csrf_passes_a_same_origin_browser_form_post_and_refuses_cross_site`, `navigations_get_errors_as_pages_and_scripts_get_json`; E2E `tests/browser_forms.rs` sends exactly Chrome's headers: login form accepted, cross-site refused as a page, script refused as JSON. Drill: one login + one form action in Chrome on CT 118 before every dashboard-touching kit release (TEST_PLAN §5). |
| K11 | Essential | **Listen address is a knob.** `--listen` / `<PREFIX>_LISTEN` / `listen =`; documented default `0.0.0.0:8080`; the bound address is logged at startup and shown by `--print-config`; tests always ask for port 0. | E2E: start on `127.0.0.1:0`, read the port from the log line, connect. Grep-test: no literal port in the suite except 0. |
| K12 | Essential | **Clients model, store trait, three buttons.** A client = name + token (32 random bytes, hex); actions issue, re-issue, revoke (row kept, name freed), delete; tokens encrypted at rest (XChaCha20-Poly1305 under `<PREFIX>_SECRET_KEY`), compared constant-time; `ClientStore` trait with the kit's encrypted-file implementation as default; page and URL are `/clients`, label overridable; row buttons: reveal for `<PREFIX>_REVEAL_SECONDS` (default 10), copy token only, copy full curl command; destructive buttons arm-before-act; busy state on slow buttons (rule 31). | E2E: full lifecycle over HTTP; plaintext-scan: the token appears in no page HTML except the reveal endpoint's response; a revoked token is refused within the same second; a second `ClientStore` (in-memory) passes the same suite (rule 7g). |
| K13 | Essential | **Last requests per client, last used.** The kit keeps the last N (`<PREFIX>_CAPTURE_KEEP`, default 20) requests per client token: method, path, redacted headers (Authorization, Cookie, anything matching a redaction list), body truncated at `<PREFIX>_CAPTURE_BODY_BYTES`, kept for `<PREFIX>_CAPTURE_TTL_SECS` (default 3600); shown expandable on the client's row; per token `last_used_at` and a request counter. Replaces Almanac's captures page and capture-only token. | E2E: three requests with a token → three rows, Authorization shows `***`, a body above `<PREFIX>_CAPTURE_BODY_BYTES` shows the truncation notice (bodies above `<PREFIX>_MAX_BODY_BYTES` are refused with 413 before capture — amended 2026-09-05, H15: the earlier "2 MB body" could never pass the 1 MiB default cap); after TTL the rows are gone; `last_used_at` moves (rule 22: shown to take two values). |
| K14 | Essential | **Send a test request.** A button per client row sends one request with that client's token to a project-declared test route with a project-declared sample body, so the request appears in K13 immediately. Never leaves the process. | E2E: click → a K13 row exists with `source=test`; a project without a declared test route shows no button. |
| K15 | Essential | **kp-themes and chrome from one place.** The kit vendors kp-themes v3.1.0 (checksum-gated in the kit's CI), serves the theme picker, no-flash snippet, skip link, `data-kp-confirm` / `data-kp-busy` behaviours; static assets embedded with `include_str!` and served with a content-hash `?v=` and a one-year immutable cache header. | CI gate: vendored files match the release's SHA256SUMS. E2E: `/static/<file>?v=<hash>` returns `Cache-Control: immutable`; a changed asset changes the hash. |
| K15 (amendment 2026-09-09, kp-themes 5.0.0) | — | **The kit vendors kp-themes 5.0.0** under the package's own paths (`/static/kp/{dist,css,js,fonts}/`): the release bundle `dist/kp-themes.css` (25 themes, components, layout + utility classes — the templates drop their inline layout styles — and all 25 registers, each scoped to its theme; D2 2026-09-09 replaced a per-theme register loader), the package's fonts (5 MB, OFL), the six-module JS closure plus `js/effects.js` (R2-b: the terminal block cursor and the themes' arrivals); the minified `dist/` twins deliberately not. Renamed themes (`topo`/`tazhib`/`nishiki`) are migrated in `theme-boot.js`. Confirmations are kp-themes' native `<dialog>`. | `vendored_kp_themes_match_the_manifest` (142 files), `theme_list_comes_from_the_vendored_registry` (25), `serve_sets_immutable_cache_and_404s_unknown` (hash → a year, bare → a day), inbox E2E `dashboard_pages_render_with_layout_and_assets` (fonts.css, the bundle, effects.js, a woff2); `vendored_javascript_imports_only_vendored_modules` (closed import graph). |
| K16 | Essential | **Layout with extension hooks and an explain block.** One minijinja layout with named blocks; a project registers nav entries, extra pages, status-page sections, and extra columns/actions on the client row; every section carries an explain block (short plain-language text above the table). | E2E: the example registers one page, one status section and one client column and all three render; a page without an explain block fails a template lint test. |
| K16 (amendment 2026-09-06, S1) | — | **Issue-form fields and hooks:** `client_form_field` (text or select, options at render time), `on_client_issued` (before the token exists; may refuse) and `on_client_deleted` (before the delete; may refuse). Kenny's ruling on N1: a project's per-client extras (Almanac: the calendar) ride on the kit's page instead of a second page — "dat was toch het hele concept van kit". | E2E `tests/client_form_hooks.rs`: the field renders with current options, a refusal issues nothing, an accepted issue hands name and fields to the hook, a duplicate name never reaches it, a refused delete deletes nothing, an accepted one tells the project. |
| K17 | Essential | **Status page and problems card.** Version, uptime, health per subsystem, the update card (current version, latest release, last check, mode, hold, rollback state), a problems card the project feeds (configuration that failed to load, with reason and a reload action), then project sections. | E2E: every field on the status page is shown to take two values in the suite (rule 22); the update card shows "off" and "supervised" in two runs. |
| K18 | Essential | **Self-update module, three modes.** `off` / `supervised` / `autonomous`, exactly one armed, container detection forces `off`. Supervised = `<name> update`: one attempt, fetch VERSION → SHA256SUMS + .minisig → binary, verify signature then hash, probe with `--check`, rename swap, exit 0 (also when already current), never restart, no state written. Autonomous = a timer (`<PREFIX>_UPDATE_INTERVAL_SECS`), same pipeline, self-restart, rollback marker restores the previous binary when the new one does not report healthy within `<PREFIX>_UPDATE_HEALTHY_AFTER_SECS` over `<PREFIX>_UPDATE_MAX_START_ATTEMPTS` starts; one log line per cycle at info (rule 23). Starting code: Almanac's `core/update.rs` + `shell/update.rs`. | Unit: version comparison, decision table (9 cases from Almanac). E2E with a local fake release server: supervised update swaps the file and exits 0; second run exits 0 without touching the file (hash equal); autonomous rollback restores the previous binary after a probe failure. Paused-clock test for the first tick (Almanac lesson). |
| K19 | Essential | **Release layout and signing.** Every release: `<name>`, `SHA256SUMS`, `SHA256SUMS.minisig`, `VERSION`; CI builds and uploads the first two with `GITHUB_TOKEN`; `chassis release` (K23) signs locally with the one ecosystem minisign key; the public key is compiled into the kit and is the only trust anchor the updater uses. | Regression vector: one real signature made with Kenny's key over a fixed manifest (rule 9). Unit: a manifest with a wrong signature is refused before any hash is read. |
| K20 | Essential | **Hold, drill flag, version check when off.** `<PREFIX>_UPDATE_HOLD=<version>` pins or skips; `<PREFIX>_UPDATE_DRILL=broken` makes the updater install a release that verifies but exits non-zero on start (for the homelab's broken-release drill); in mode `off` the kit still fetches VERSION and shows it on the update card. | E2E: hold prevents a swap that would otherwise happen; drill installs a marked-broken binary and the rollback path (K18) recovers; in `off` the card shows a newer version without swapping. |
| K21 | Essential | **State copy before a swap, one-version-back migrations.** Before any binary swap the kit asks the project for a consistent copy of its state (hook; kyu's `VACUUM INTO` is the reference) into `<state>/pre-update/<version>/`, pruned to `<PREFIX>_UPDATE_KEEP_COPIES`; the kit's own store format carries a version and stays readable by the previous minor version; the scaffold ships a test template asserting that. | E2E: a swap leaves a copy; a store written by version N opens in a build of N-1 (test in the kit for its own store). |
| K22 | Essential | **Notifier as per-event webhooks.** Fixed kit events (`update.ok`, `update.failed`, `update.rolled_back`, `health.degraded`, `service.started`) plus project events; per event zero or more webhooks (URL, method, headers, body template with event fields; secrets from env via `${VAR}`); presets `kyu-topic` and `ha-webhook`; optional fallback chain; delivery never blocks the caller. | E2E with a local receiver: event → one POST with rendered body; primary down → fallback receives; a header value from `${VAR}` never appears in logs. |
| K23 | Essential | **The `chassis` scaffold command.** `new <name>` writes Cargo.toml, main.rs, CI + release workflows, Dockerfile, systemd unit (golden), `service.yml`, sign script, `.githooks`, `deny.toml`, `rust-toolchain.toml`, README skeleton, and creates + configures the GitHub repo via `gh` (public, branch protection with required checks once CI has run); `sync` shows per-file diffs against the current scaffold; `release <version>` bumps, tags, waits for the tag's CI run, downloads the artefacts, verifies checksums, signs locally, uploads `.minisig` + `VERSION`, and prints what it did. | E2E: `new` in a temp dir produces a project that passes `cargo build` and its own gates; `sync` reports zero diffs right after `new` and one diff after a template change; `release` is drilled against a scratch repo (rule 14) with a dry-run flag for CI. |
| K24 | Essential | **Readable for a beginner.** `examples/minimal.rs` (one file, ≤40 lines), rustdoc opening every module in plain English, a concrete API (no generic builders), an `ARCHITECTURE.md` with the eject route (which module to copy for what). C4 in SCOPE.md is the acceptance test. | Doc test: every public module has a doc comment starting with a sentence, checked by a lint test; the minimal example compiles as a doc/example target in CI. |
| K25 | Essential | **Test helpers, semver, consumer builds.** `chassis::testing`: start an app in-process on port 0, a `Clock` trait with a paused implementation, a fake release server; semver over the Rust API and the scaffold file shapes with migration notes in CHANGELOG.md; the kit's CI builds the example and, once they exist, the migrated consumers against the release candidate. | The kit's own suite uses only the helpers; `cargo semver-checks` runs in CI (informational until 1.0); CHANGELOG lint: a major bump without a "Migration" heading fails. |
| K25 (amendment 2026-09-06, M1–M3) | — | **Scaffold sync leaves room for the project:** `gates.sh`/CI run `.claude/hooks/gates.project.sh` when present (M1); `.chassis.toml` `env_file` and `latch_env` reach the deploy templates (M2); `.gitignore` entries under the marker survive a sync (M3). Found by the first `chassis sync --write` on a migrated project (Almanac): it would have dropped two project gates, rewritten the measured CT 112 unit with scaffold defaults, and lost two ignore rules. | unit `measured_env_file_and_latch_env_reach_the_deploy_files`, `gitignore_additions_under_the_marker_survive_a_sync`; the scaffold E2E runs the generated `gates.sh` (which tolerates a missing project hook). |
| K26 | Essential | **Example service `inbox`.** Clients POST JSON to `/v1/messages`; a dashboard page lists messages; a status section shows the count; a `message.received` project event fires the notifier. Used for the field test, the drills and C1–C4. | E2E: the whole inbox suite runs against the built binary; the field test on CT 118 posts one message with a client token and sees it on the page. |

## Round 2 — Claude's proposals (W)

| ID | Rating | Feature | Test bar |
|---|---|---|---|
| W1 | Essential | **Request-id.** Every request gets `x-request-id` (accepted from the proxy if present, else generated), carried in every log line of that request and echoed in the response. | E2E: a supplied id is echoed; a missing one is generated; the access log line contains it. |
| W2 | Desired | **clap for the CLI, one backoff helper.** clap 4 derive for all commands and flags (unknown flags refused); a backoff-with-jitter helper (base, cap, jitter) used by the updater and offered to projects. | Unit: backoff sequence within bounds over 1000 samples; `--help` snapshot test. |
| W3 | Essential | **Distroless self-probe.** Folded into K7: `--healthcheck [url]`. | see K7 |
| W4 | Desired | **Quiesce and backup-paths contract.** `<name> quiesce --for <secs>` (or an admin endpoint) pauses writers and fsyncs; `<name> backup-paths` prints the directories the nightly must take; the homelab builds the caller once this exists. | E2E: during quiesce a write blocks and completes after; `backup-paths` lists exactly the state root's durable dirs, never caches. |
| W5 | Later | **Token scopes.** Per-client route allow-lists. Registered, no hook in the kit. | — |
| W6 | Don't do | **Unprotected mode with banner.** Kenny 2026-09-05: the kit never starts a dashboard without both secrets; kyu loses this behaviour when it migrates. | — |
| W7 | Don't do | **`/readyz`.** The homelab does not need it; `/healthz` carries subsystem status. | — |
| W8 | Don't do | **In-process TLS.** TLS is Traefik's job (C2 in the deep-dive round). | — |
| W9 | Essential (proposed during AFK, ratify in R2) | **systemd readiness notify.** The golden unit uses `Type=notify`; the kit sends `READY=1` after the socket is bound and the stores opened, so `systemctl is-active` — the only health signal the homelab's update supervision reads today — means "bound and serving", not "process exists". Falls back silently when `NOTIFY_SOCKET` is unset. | E2E on CT 118: `systemctl start` returns only after `/healthz` answers; a unit with `ExecStart` pointing at a binary that fails `--check` never reaches active. Unit test: no `NOTIFY_SOCKET` → no error. |

## Mandatory items (M)

| ID | Item | Answer |
|---|---|---|
| M1 | **Update & distribution** | Built-in, signed self-update with three modes (K18–K21); the kit itself is distributed as a git dependency pinned to a tag with SHA256SUMS beside the tag (N3 in SCOPE.md); the `chassis` binary is a release asset like any service. |
| M2 | **Ecosystem integration** | **kyu**: notifier preset `kyu-topic` (K22); no other coupling. **latch**: the golden unit supports `ExecStart=latch run --env prod -- <binary>` as an alternative to `EnvironmentFile`; the kit reads env only, never `.env`. **homelab**: `service.yml` from the scaffold, supervised update contract, `/healthz` version field, one state root — G4. **kp-themes**: vendored v3.1.0 in the kit (K15). **HTTPSwitchboard / kyu-runner / Almanac**: consumers-to-be, not dependencies. |
| M3 | **Backup & restore** | State = the encrypted client store, sessions and passkeys (one file each under `<state>/`), the project's own data, the config file if it lives under the root, and the pre-update copies (K21). Regime: the homelab's nightly tar of `data_dirs` (live today; quiesce via W4 later). **Restore drill:** on CT 118, before the 1.0 release: destroy the state, restore the tar, start, log in with an existing client token — queued as measurement in docs/PENDING_MINI_ROUNDS.md. |
| M4 | **Data & config location** | One root `<PREFIX>_STATE_DIR` (default `/var/lib/<name>`; the homelab's `service.yml` sets `/appdata/<stack>/<name>-config`); config file at `<root>/config.toml` unless `--config` says otherwise; secrets only via env / EnvironmentFile / `latch run`; caches (update staging) live **beside the binary**, not under the root, because the rename swap needs the same filesystem; logs go to stderr → journald. Written into ARCHITECTURE_DECISIONS.md as the configuration surface. |

## Tally

Essential 27 (K1–K26, W1; W3 folded into K7) · Desired 2 (W2, W4) · Later 1 (W5) · Don't do 3 (W6–W8).

Freeze: ratified in R2.

## Round 3 — kit candidates after the migrations (F1–F8, weighed 2026-09-06)

Proposed by Claude after the four migrations (chassis-rs first, then
Almanac, kyu, kyu-runner, http-switchboard), each grounded in something
that pinched that day; weighed by Kenny in the F1–F8 form (seven
Essential, F3 sent to a deep-dive and then rated Later). The deep-dive
found that the homelab already records what F3 wanted to measure
(`stacks/<stack>/service.yml` in the homelab repo), so the ssh variant is
out of scope (G4) and the read variant is registered under F3 for later.
Built as **batch 3** (REALIZATION_PLAN L9) in five parallel worktrees;
release timing is Kenny's call (form R1, 2026-09-07: "zie ik zelf nog wel").

| ID | Form | Rating | Feature | Test bar |
|---|---|---|---|---|
| K25 (realized) | F1 | Essential | **`chassis::testing` harness.** `TestApp` starts an `App` on port 0 with generated secrets in a temp state dir; `login()`, `issue_client(name, fields)`, `bearer()`, `page()`, `json()`, and `as_browser()` (Chrome's form-post fingerprint from CF-7) so a project tests its dashboard forms like a browser does. | The kit's own in-process suites use only the harness; each helper has a test that goes red without the behaviour it wraps. |
| K27 | F2 | Essential | **Generated kit documentation per project.** `chassis sync` renders a kit-owned `docs/KIT.md` with the project's name and prefix filled in: the door and the tokens, the dashboard pages, `/healthz` and `/metrics`, self-update and the drills, notifications, and the knob table (K31). Project docs point at it instead of retelling it. | Scaffold E2E: a new project has `docs/KIT.md`; a unit test asserts every knob key appears in it. |
| K28 | F4 | Essential | **Vocabulary per project.** `App::vocabulary(singular, plural)` (default `client`/`clients`) reaches every kit text about clients: headings, explanations, empty state, buttons, error messages, the curl hint. `clients_label` keeps naming the nav entry. | Rendering the clients page with `source`/`sources` leaves no "client" in the visible text (attributes and API paths excepted). |
| K29 | F5 | Essential | **Project actions on rows and sections.** `ClientAction { label, route with {id}, method, destructive, confirm }` registered via `App::client_action`; `StatusSection::actions()` (default empty) puts buttons under a status section. The kit renders them through the existing `data-post` mechanism with arm-before-act and busy state (rule 31); a refusal from the project route is shown to the user. | A registered action sits on every active row and posts to the substituted route with the client id; a destructive one carries the confirm attributes; a section action renders on `/`; a refusing route's remedy is visible. |
| K30 | F6 | Essential | **`chassis clients` — tokens without a browser.** `chassis clients list\|issue\|reissue\|revoke\|delete --url <base> --token-env <VAR> [--field k=v] [--json]` drives the same clients API a dashboard uses, with the admin token from the environment, never argv. Serves the headless services (http-switchboard, kyu-runner). | E2E against an in-process app: issue via the CLI → the token works on an API route → revoke via the CLI → 401. |
| K31 | F7 | Essential | **Knob table from the spec.** Every `Knob` carries its meaning; `AppSpec::knobs_markdown()` renders name, env var, flag, default, meaning, secret; `<name> --knobs` prints it; K27's KIT.md embeds it. | The rendered table names every key `knobs()` knows; the scaffold E2E compares it with the generated document. |
| K32 | F8 | Essential | **`chassis sync` reports non-file drift.** The kit tag in Cargo.toml vs `chassis_tag`; `kp_themes` vs what the CLI's kit vendors; with `--remote`, the branch protection's required checks vs the CI job names (`--protect` repairs). Each difference is one line with its remedy. | Unit tests per comparison; a fresh project reports "in sync". |
| K25 (amendment 2026-09-07, D4) | — | — | The harness also has `start_with_env` (a project injects its own knobs, as Almanac's and kyu's hand-written harnesses did) and `start_open`/`try_start_open` (needed to migrate `open_dashboard.rs`); the `Clock` trait with a paused implementation named in the K25 row is **not built** (not in the F1 ratification; open). `as_browser()` sends Chrome's current form-submit headers; the CF-7 `Origin: null` case lives in `browser_forms.rs` (D3). | `k25_*` in `crates/chassis/tests/testing_harness.rs` |
| K28/K29 (amendment 2026-09-07, D4) | — | — | One shared `Action` struct with aliases `ClientAction`/`SectionAction`; builders `.method()` and `.busy_label()` beside the ratified `post()`/`destructive()`. Found and fixed on the way: a refusal on a `[data-post]` button was never visible (chassis.js). | `k29_*` in `crates/chassis/tests/vocabulary_and_actions.rs` |
| K30 (amendment 2026-09-07, D2/D4) | — | — | Verb `reveal`; `reissue` prints the new token; a wrong Bearer on admin routes answers JSON 401 with a remedy instead of a redirect (no-credential requests still redirect) — kept by Kenny (D2). | `k30_admin_bearer_manages_clients_and_a_wrong_bearer_gets_401_not_a_redirect` |
| K31 (amendment 2026-09-07, D4) | — | — | `Knob.feature` names the feature that reads a knob; the AR3 table in ARCHITECTURE_DECISIONS stays a coarse summary, `--knobs` is the source. | `k31_*` |
| K32 (amendment 2026-09-07, D1) | — | — | `sync` exits 1 on any drift still unresolved when the run ends — also under `--write` (the kit tag in Cargo.toml, a protection without `--protect`); `kp_themes` is compared and corrected before rendering because `docs/KIT.md` names it. | unit `new_writes_a_project_that_syncs_clean` (D1 block), E2E `a_new_project_compiles_and_answers_version` (exit 1 under `--write`) |
| K34 | D5 | Essential (batch 4) | **A kit test ships with every new project.** `chassis new` writes `tests/kit_smoke.rs` on `chassis::testing` (login, issue a client, one API call with the token, one page) and puts `features = ["testing"]` on the dev-dependency; consumers drop their hand-written `tests/common/mod.rs` when they adopt 1.8.0 (their own sessions, rule 7a). Kenny 2026-09-07: Opnemen. | Scaffold E2E: the new project's own gates run the smoke test green; `chassis sync` keeps the file (kit-owned). |
| K33 | F3 | Later | **Deploy knobs from the homelab's stack file.** `homelab_stack = "<stack>"` in `.chassis.toml`; sync derives `state_dir`, `env_file`, `latch`, `latch_env`, `vmid` from `stacks/<stack>/service.yml` in the homelab repo and reports drift; `--write` adopts. Kenny 2026-09-07: Later. | — |

Tally after round 3: Essential 35 (K34 added 2026-09-07) · Desired 2 · Later 2 (W5, K33) · Don't do 3.

## Round 4 — what the consumers found in 1.8.0 (weighed 2026-09-09)

kyu, Almanac and kyu-runner adopted 1.8.0 the day it was released and each
reported back through Kenny. kyu's and Almanac's finding was a fault and was
fixed the same day (CF-12, `docs/CORRECTIONS.md`); the rest became this
round. kyu-runner is the first HEADLESS consumer to go through a kit batch
(`default-features = false`, features `core` + `self-update`; measured on its
running binary: `/healthz` 200, `/metrics` 200, `/login` 404, `/clients` 404),
and three of the four items are one pattern: the kit describes itself to a
project that builds only part of it.

| ID | Rating | Feature | Test bar |
|---|---|---|---|
| K35 | Essential (batch 4) | **The generated documentation describes what the project builds.** `chassis sync` resolves the kit features from the `chassis` dependency in the project's `Cargo.toml` (implications expanded, `default-features = false` honoured), names them under the title of `docs/KIT.md`, and renders only the sections that binary carries; `chassis new` renders the same list into the `Cargo.toml` it writes. Where no dependency can be read, the document keeps every section and sync says so — "could not tell" is not "no features" (rule 30). Kenny 2026-09-09: Onmisbaar. | A headless render carries no door, no dashboard, no notifications, no `gen-secret` row and no dashboard knob, and keeps self-update and every core knob; unit tests on the feature resolution incl. `passkeys` → `dashboard`. |
| K36 | Essential (batch 4) | **`--knobs` lists the knobs this binary can act on.** `AppSpec::knobs_markdown` filters on the compiled features; `knobs_markdown_for(features)` renders for a named set, which is what the CLI uses (it is built with `core` alone). `knobs()` and `knob_keys()` stay complete, so a knob of an absent feature is still accepted and ignored in the config file. Kenny 2026-09-09: Onmisbaar. | A core-only table has `LISTEN` and not `TOKEN`; `knob_keys` still carries every key; `--knobs` equals the table of `compiled_features()`. |
| K37 | Essential (batch 4) | **`docs/MIGRATION.md` says who the test harness is for.** The harness adoption is for services WITH a dashboard, with both reasons kyu-runner measured: `testing` implies `dashboard`, and `TestApp` runs in-process, so a suite that reads the service's log, signals it or checks its exit code cannot use it (29 tests there, kept by Kenny's decision). Kenny 2026-09-09: Onmisbaar. | Documentation only; the claim is checked against kyu-runner's own `docs/TEST_PLAN.md`. |
| K38 | Desired (batch 4) | **A client token from ANOTHER service, as a library.** `chassis::admin::AdminApi` (part of `core`, so a headless service reaches it without a dashboard): `new`/`from_env`, `issue_client(name, fields)`, `list_clients`, `client_id`, `reveal_token`, `revoke_client`, `delete_client`. kyu-runner hand-wrote thirty lines of this to test against a real kyu hub; http-switchboard is in the same position. Kenny 2026-09-09: Gewenst. | A real round trip over HTTP against a service the harness started: issue, list, find by name, reveal, revoke; a wrong admin token is `Unauthorized` with a remedy and never a login page; an unreachable service names the URL. |
| K34 (realized) | Essential (batch 4) | **A kit test ships with every new project** (rated 2026-09-07, built 2026-09-09). `chassis new` writes `tests/kit_smoke.rs` on `chassis::testing`; the harness is a dev-dependency; the file is kit-owned so `sync` keeps it. Amendment from the build: a project without the `dashboard` feature gets neither the file nor the dev-dependency, because the test logs in and `testing` implies `dashboard` — found while landing K34 beside K35. | Scaffold E2E: the file is written, the generated project's own gates print the smoke test green, `sync --write` restores it after vandalism; unit tests for kit-ownership and for the headless skip. |
| HK5 | Adopted (both halves) | **A kit migration makes a project's own documents lie.** `docs/MIGRATION.md` carries, per version, a list of claims a project should re-check in its own README and runbook; the general lesson goes to the batch 3 retrospective as a candidate for the procedure. From kyu: its README named a workflow file replaced at 3.0.0 and its runbook said kyu ships no self-updating binary, both false for months, neither visible to `chassis sync`. Kenny 2026-09-09: Allebei. | The 1.8.0 section lists five claim shapes; the retro decides the procedure half. |

Tally after round 4: Essential 38 · Desired 3 · Later 2 (W5, K33) · Don't do 3.

## Round 5 — rule 46, the consumer reports and the name Clients (weighed 2026-09-10)

Kenny's own requirement framed this round: the kit is plug and play, a
consumer picks features and it works, and the concept a project registers is
called **Clients** everywhere. Six of the sixteen items came back asking for
a proper explanation and were answered with measurements before they were
rated; those measurements are in `docs/PENDING_MINI_ROUNDS.md` §Round 5.

New identifiers follow the house scheme set on 2026-09-09 (`feat-<domain>-<n>`,
`fix-<n>`, `arch-<n>`); the rows above keep the names they were born with.

| ID | Rating | Feature | Test bar |
|---|---|---|---|
| feat-clients-1 (realized) | Essential | **The kit says Clients.** Every surface the kit renders or prints names the concept `client`, or the project's own vocabulary where one is set: the clients page, the Revoke confirmation, the login page, the generated `docs/KIT.md`, the kit's own documents and the CLI's help. The guard forbids `caller` as well as `client` and reads every `data-kp-confirm` text, because attributes are where the last one hid. | `k28_the_pages_speak_the_vocabulary_and_never_say_client` with the widened word list and the confirmation texts; both halves drilled red. |
| fix-3 (realized) | — | **`update_cmd` reproduces the unit's environment.** One `--property=Environment=KEY=VALUE` per `Environment=` line the unit declares, in its order; the comment no longer promises more than the command does. | The deploy test reads both templates: a variable declared in the unit and missing from `update_cmd` is red. Drilled red by removing the two lines. |
| feat-ci-1 (realized) | — | **CI runs on every branch.** Restoring what the narrowing broke; the reasoning and the measurements are in the commit and in the queue. | Proven by firing: the landing branch produced a green check and main accepted the push. |
| feat-clients-2 | Essential | **The kit stores a client's declared fields.** A project declares a field and nothing else: the kit keeps the values with the client in its sealed store, shows them, returns them from the API, accepts them from `chassis clients issue --field`, and removes them with the client. `on_client_issued` stays for refusal. The clients-store format goes up one version, read back compatibly. | A declared field survives a restart, appears on the row and in the API, reaches the CLI, and is gone after a delete; the previous store version still reads. |
| feat-build-1 | Essential | **One build shape for everyone: static musl on distroless.** The scaffold builds `x86_64-unknown-linux-musl` with the target pinned in `rust-toolchain.toml` (not a separate `rustup target add`, which silently installs against another toolchain), runs on `gcr.io/distroless/static:nonroot`, and a CI step asserts the binary has no dynamic references. `passkeys` leaves the default feature list because it pulls OpenSSL, which musl would have to vendor per release; no consumer builds it today. | `ldd` on the release artefact shows no dynamic links, drilled red against a glibc build; the scaffold E2E still builds and passes its own gates. |
| arch-1 | Essential | **The build target's premise becomes a dependency, and the release publishes its floor.** A dated amendment rewrites "every target environment runs glibc 2.41 or newer" as a dependency with what happens when a machine falls behind; the release states the minimum the artefact needs, so the homelab's deploy check compares against a published number instead of reasoning from `objdump`. | The generated document and the release notes carry the floor; a unit test pins that the document states it. |
| feat-sync-1 | Essential | **`chassis upgrade <version>`.** Aligns `.chassis.toml`, the dependency and the dev-dependency, updates cargo, runs the gates. `sync` still never writes Cargo.toml. | A project two versions behind is current after one command, with the gates green; a dev-dependency left behind is reported. |
| feat-api-1 | Essential | **A public-surface snapshot with its check.** Every public signature in a file in the repository; the check fails when the code drifts from it without the version saying so. Runs in the gates Kenny starts and as a precondition in the release script, never at a commit. | Adding a public function makes the check fail until the snapshot is updated; drilled red. |
| feat-api-2 | Desired | **A transition window.** A new shape stands beside the old for one version with a deprecation marker, plus a list of what disappears when and a check that the list promises nothing already gone. Built at the first breaking change, not before. | The list and its check; a deprecated item still compiles and warns. |
| feat-dep-1 | Not now, with its trigger written down (Kenny, 2026-09-10) | **The dependency itself.** Costed and declined for now. What a registry would need, measured: exactly one crate (the kit has no path or git dependencies of its own), 5.2 MiB per version, a static file server with a `config.json` and one index file per crate — the publish endpoint is only needed for `cargo publish`, and the release script can write the index line itself. No authentication at all, because this repository is already public. What decides it: all four consumers build CI on GitHub-hosted runners, so a LAN-only registry is unreachable and the service would have to be internet-facing and up whenever any of five repositories runs CI. **The trigger that reverses this:** a shared library crate that itself builds on the kit — then a project resolves two kits side by side and a git tag stops working. crates.io was measured and set aside separately: the name is taken (0.2.0, 5202 downloads), the package holds no secrets, and publishing would make the consumer check more important rather than less, because `cargo update` would then move a consumer without anyone touching the kit line. | — (no code; the costing and the trigger are the deliverable) |
| feat-config-1 | Essential | **The kit splits the config file.** A project asks for its own part and gets it, table sections included, so `remove("notify")` stops being knowledge someone has to carry. | A project's own parser with `deny_unknown_fields` accepts what the kit hands it; a new kit table section is stripped too, proven by a test that adds one. |
| feat-metrics-1 | Essential | **A counter and a gauge that format themselves.** Enough to take Prometheus text formatting out of every consumer; label values escaped by the kit, since one unescaped value invalidates the whole scrape. | A label value containing a quote and a backslash still produces a valid exposition; the source plugs into the existing registry. |
| feat-testing-1 | Essential | **One assembly for the service and its tests.** The harness takes the project's configure function as a whole, so a test cannot run a smaller assembly than the real start, and ships usable test secrets. | A test that forgets a registration is impossible because there is one function; the harness's own suite proves the secrets work. |
| feat-docs-1 | Essential | **What a feature brings with it, written down.** The feature chain with its weight in the generated document, and per feature the secrets it makes mandatory and what fails on a machine without them. | The generated document names the chain; the migration guide names the secrets per feature. |
| ask-1 | Adopted (both halves) | **The badge that breaks a word.** Asked of kp-themes with the measured selector, plus one line in the kit's own stylesheet keeping the state column whole until their release. | The state cell does not wrap; the request is relayed with its measurement. |
| feat-clients-3 | Don't rename | **`Caller` keeps its name.** It answers who sent a request, and a client is one of its two shapes; the generated document says so in one line. | Documentation only. |

Tally after round 5: Essential 48 · Desired 4 · Later 2 (W5, K33) · Don't do 4 · Open investigation 1 (feat-dep-1).
